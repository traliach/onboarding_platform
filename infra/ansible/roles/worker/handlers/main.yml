---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart redis6
  ansible.builtin.systemd:
    name: redis6
    state: restarted

- name: Restart onboarding-worker
  ansible.builtin.systemd:
    name: onboarding-worker
    state: restarted
