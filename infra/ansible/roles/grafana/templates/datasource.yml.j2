apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ hostvars[groups['prometheus'][0]]['private_ip'] }}:9090
    isDefault: true
    editable: false
