global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: node
    static_configs:
      - targets:
{% for host in groups['all'] | sort %}
          - "{{ hostvars[host]['private_ip'] }}:9100"
{% endfor %}
        labels:
          fleet: onboarding_platform
