---
# Handlers for the common role. Handlers run at most once per play,
# at the end, triggered by `notify` in tasks.

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart node_exporter
  ansible.builtin.systemd:
    name: node_exporter
    state: restarted
