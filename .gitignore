# Dependencies
node_modules/
.pnp
.pnp.js

# Environment
.env
.env.*
!.env.example

# Build outputs
dist/
build/
out/
.next/
coverage/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# AI session config (local-only governance files)
CLAUDE.md
.cursorrules

# Personal / local-only documentation
# Kept on disk but never pushed — these are the author's private
# working notes, not project artifacts.
prompt.MD
docs/Onboarding platform build story.MD
docs/deploy.md

# Transient curl / dev artifacts
cookies.txt

# Ansible — decrypted vault, local password files, installed collections,
# inventory cache, virtualenv. The .example files ARE committed.
infra/ansible/group_vars/all/vault.yml
infra/ansible/inventory/hosts.yml
infra/ansible/.vault-pass
infra/ansible/collections/
infra/ansible/.venv/
/tmp/onboarding-platform-ansible-cache
